const router = require('express').Router()
//TODO check book name from thirdparty api and get information, including image
router.get('/api/books/:id', (req, res) => {})

router.post('/api/books/', (req, res) => {})

module.exports = router
